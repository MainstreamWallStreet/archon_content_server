# Dockerfile for headless Langflow runtime
FROM langflowai/langflow-backend:latest

ENV LANGFLOW_BACKEND_ONLY=true \
    LANGFLOW_PORT=7868

# Copy bundled flows
COPY src/flows/*.json /app/flows/

EXPOSE 7868

CMD ["langflow", "run", "--host", "0.0.0.0", "--port", "7868"] 