# ==============================================================================
# Zergling FastAPI Server Environment Variables
#
# - This file is a template. Copy it to `.env` for local development.
# - Do not commit the `.env` file to version control.
# - Variables set in the environment will override values in this file.
# ==============================================================================

# --- Application Settings ---
APP_NAME="zergling"
LOG_LEVEL="INFO"
DEBUG=false
ENV="dev" # "dev", "staging", or "prod"

# --- Authentication ---
# API key required for all endpoints except /health.
# In production, this should be set in Google Secret Manager.
ZERGLING_API_KEY="your-secret-api-key"

# --- Google Cloud Platform ---
# Your GCP Project ID.
GOOGLE_CLOUD_PROJECT="your-gcp-project-id"

# The GCS bucket used for application data storage.
EXAMPLE_BUCKET="your-gcs-bucket-name"

# --- Local Development Only ---
# Path to your GCP service account JSON file for local authentication.
# This is NOT used in production (Cloud Run uses the attached service account).
GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/gcp-service-account.json"

# ==== Storage ====
STORAGE_BUCKET=your-app-data-bucket

# ==== Server Configuration ====
HOST=0.0.0.0
PORT=8080
