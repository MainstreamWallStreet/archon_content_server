apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: banshee-pipeline                 # ← new pipeline
description: Promote Filing‑Fetcher API to Cloud Run
serialPipeline:
  stages:
    - targetId: dev
